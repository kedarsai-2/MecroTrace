# Copy to .env.local for local overrides (not committed).
# Local backend (default when VITE_API_URL is unset):
# VITE_API_URL=http://localhost:8080
#
# UAT backend (see .env.uat — used by npm run build:uat):
# VITE_API_URL=https://uat-merco.qualityoutsidethebox.org
